# Test-only dependencies — never installed into the production Docker image
# (Dockerfile only installs requirements.txt). Install locally with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
